version: '3.1'
services:
  rmq:
    image: rabbitmq:3-management
    restart: always
    ports:
      - "15672:15672"
      - "5672:5672"
  redis:
    image: 'redis:latest'
    ports:
      - '6379'
    restart: unless-stopped
